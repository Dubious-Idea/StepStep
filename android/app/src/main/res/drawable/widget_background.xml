<?xml version="1.0" encoding="utf-8"?>
<!-- Graphite card with a hairline edge — the widget brings its own surface so
     the neon ring reads as light against it, whatever wallpaper is behind. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/brand_surface" />
    <stroke
        android:width="1dp"
        android:color="@color/brand_stroke" />
    <corners android:radius="@dimen/widget_corner_radius" />
</shape>
